<!-- NOTE: This template will generate each page per section (if multipage)
or the single page with all sections of the documentation.

You should use Handlebars templating syntax with the template API (refer to API Reference
in the documentation for more info) to plug in all the contents of the documentation into this page. -->

<!-- NOTE: All links to theme assets or user assets should be prefixed with "{{rootPrefix}}/assets/theme/". -->

<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    {{! Set dynamic favicon which responds to dark theme by using the prefers-color-scheme media query }}
    <link rel="icon" type="image/svg+xml" href="{{rootPrefix}}/assets/theme/images/favicon.svg">
    <link rel="mask-icon" color="white" href="{{rootPrefix}}/assets/theme/images/favicon.svg">
    {{! Set the page title }}
    <title>{{title}}{{#if pageTitle}} - {{pageTitle}}{{/if}}</title>
    <link rel="stylesheet" href="{{rootPrefix}}/assets/theme/styles.css">
    <style>
      blockquote:before {
        {{! background-image is set here to benefit from rootPrefix variable }}
        background-image: url('{{rootPrefix}}/assets/theme/images/icon-note.svg');
      }
    </style>
    {{! Load scripts }}
    <script src="{{rootPrefix}}/assets/theme/scripts/steroids.js"></script>
  </head>
  <body>
    <header>
      <svg class="button-menu" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 50 50" onclick="toggleLeftPane()">
        <g transform="translate(-75 -25)">
          <rect width="50" height="50" transform="translate(75 25)" fill="none"/>
          <line x2="35" transform="translate(82.5 40)" fill="none" stroke-linecap="round" stroke-width="3"/>
          <line x2="35" transform="translate(82.5 50)" fill="none" stroke-linecap="round" stroke-width="3"/>
          <line x2="35" transform="translate(82.5 60)" fill="none" stroke-linecap="round" stroke-width="3"/>
        </g>
      </svg>
      <img src="{{rootPrefix}}/assets/theme/images/logo-header.svg" alt="Steroids">
    </header>
    <div class="main">
      <div class="left-pane">
        <nav>
          {{#each sections}}
            <a class="nav-level-{{level}}{{#if selected}} nav-selected{{/if}}" href="{{link}}">{{title}}</a>
          {{/each}}
        </nav>
        {{#if versions}}
          <nav class="versions hidden">
            <div class="versions-header-container">
              <span>Versions</span>
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 14.243 14.243" onclick="toggleVersionMenu()">
                <g id="Close_Button" data-name="Close Button" transform="translate(2.121 2.121)">
                  <line id="Line_10" data-name="Line 10" x1="10" y2="10" fill="none" stroke-linecap="round" stroke-width="3"/>
                  <line id="Line_11" data-name="Line 11" x2="10" y2="10" fill="none" stroke-linecap="round" stroke-width="3"/>
                </g>
              </svg>
            </div>
            {{#each (sortVersions versions)}}
              <a class="nav-level-1{{#if (equal version ../version)}} nav-selected{{/if}}" href="{{link}}">{{version}}</a>
            {{/each}}
          </nav>
        {{/if}}
        <footer>
          <span>Version {{version}}</span>
          {{#if versions}}<span class="link" onclick="toggleVersionMenu()">View another version</span>{{/if}}
        </footer>
      </div>
      <div class="article-container">
        <div class="backblur"></div>
        {{#each contents}}
          {{#each content}}
            <article>
              {{{value}}}
            </article>
            {{! Only insert content separator after when not looking at the last content }}
            {{#if (isNotLastContent ../content @index)}}
              <hr class="content-separator">
            {{/if}}
          {{/each}}
        {{/each}}
        <footer>
          <div class="footer-links">
            <a href="https://npmjs.com/@chisel/steroids" class="footer-link" target="_blank">Download on NPM</a>
            <a href="https://github.com/chisel/steroids" class="footer-link" target="_blank">View source</a>
            <a href="https://github.com/chisel/steroids/issues" class="footer-link" target="_blank">Report a bug</a>
          </div>
          <span>Documentation generated by <a href="https://fluorite.js.org" target="_blank">Fluorite</a></span>
        </footer>
      </div>
    </div>
  </body>
</html>
